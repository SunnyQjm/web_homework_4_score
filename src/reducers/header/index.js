const initState = {};

const HeaderReducer = (state = initState, action) => {
    let newState = {};
    Object.assign(newState, state);
    switch (action.type){

    }
    return newState;
};

export default HeaderReducer